import { HighlightColorDirective } from './app-highlight-color.directive';

describe('AppHighlightColorDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightColorDirective();
    expect(directive).toBeTruthy();
  });
});
